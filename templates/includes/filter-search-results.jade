.issues-container.block-row.section.filter-by-replace-search
	if data.posts && data.posts.length > 0
		each post in data.posts
			.issue-container.block-xs-12.block-sm-6
				div.border-top-med
					if (post.sourceLink)
						a(href=post.sourceLinkUrl target="_blank")
							h3.issue-title= post.title
							.issue-excerpt
								!= post.excerpt
					else
						a(href=post.url).issue-container
							h3.issue-title= post.title
							.issue-excerpt
								!= post.excerpt
	else
		.container
			.row
				.col-xs-12.text-center
					if filters && filters.locale == "es"
						include ./variables-page-es
						h1.no-results= noResults
					else
						include ./variables-page-en
						h1.no-results= noResults